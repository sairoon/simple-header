/*
function wave() {
    var wave = document.querySelector(".wave");
    var width = wave.offsetWidth;
    var height = wave.offsetHeight;
    var x = 0;
    var y = 0;

    var animation = setInterval(function() {
        x += 1;
        y += 1;
        wave.style.transform = "translate(" + x + "px, " + y + "px)";

        if (x > width) {
            x = 0;
        }

        if (y > height) {
            y = 0;
        }
    }, 10);
}

window.addEventListener("load", wave);*/
